services:
  db:
    image: mysql:8.0-debian
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 
      MYSQL_DATABASE: correctrate_log_db
      MySQL_USER: ratelogger
      MYSQL_PASSWORD: 
    command: ["mysqld", "--character-set-server=utf8mb4"]
    volumes:
      - db-bk:/var/lib/mysql

  java:
    build: 
      context: .
      dockerfile: JavaDockerfile
    command: sleep infinity 
    volumes:
      - ../workspace:/workspace

volumes:
  db-bk:
